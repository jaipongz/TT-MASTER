import { ListConfig } from '../../../types/listConfig'

// Custom override for list config. Keep empty values to fallback to default config.
export const {{uiCustomListConfigName}}: ListConfig = {
  title: '',
  endpoint: '',
  columns: [],
}
